tạo giao diện book_details
    - tilte giống home_screen nhưng có nút quay lại trang chủ ở bên trái trước tilte (tile là "Chi tiết).
    - thumbnail ở ngay dưới tiltebar, căn giữa theo chiều ngang, kích thước 180x240
    - tilte ngay dưới thumbnail, căn giữa
    - tên tác giả dưới tilte, căn giữa
    - divider
    - nút thêm vào tủ sách với icon.
    - nút đọc từ đầu.
    - section mới với mô tả của book
    - list chương
    - tách rời khỏi navigation bar của app
** logic khi bấm các nút và chương chỉ để placeholder ngoại trừ nút quay lại trang chủ.
** thêm vào context các tệp model, service, provider và reposistory trước khi thực hiện yêu cầu.
